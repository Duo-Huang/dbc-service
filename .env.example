# ===========================================
# DBC 项目环境变量示例文件
# ===========================================
#
# 使用说明：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 根据实际环境修改相应的值
# 3. 在 shell 中加载: source .env (注意：项目使用 config 包加载 YAML 配置，.env 仅用于本地开发注入环境变量)
#
# 配置优先级：
# 1. 环境变量 (最高优先级)
# 2. .env 文件
# 3. config/*.yaml 文件 (最低优先级)
#
# ===========================================

# ===========================================
# 应用配置
# ===========================================

# Console 应用配置
CONSOLE_SERVER_PORT=4000
CONSOLE_DB_HOST=localhost
CONSOLE_DB_PORT=5433
CONSOLE_DB_NAME=dbc_local
CONSOLE_DB_SCHEMA=dbc
CONSOLE_DB_USER=dbc_console_writer
CONSOLE_DB_PASSWORD=dbc.local.123

# Miniprogram 应用配置
MINIPROGRAM_SERVER_PORT=3000
MINIPROGRAM_DB_HOST=localhost
MINIPROGRAM_DB_PORT=5433
MINIPROGRAM_DB_NAME=dbc_local
MINIPROGRAM_DB_SCHEMA=dbc
MINIPROGRAM_DB_USER=dbc_miniprogram_writer
MINIPROGRAM_DB_PASSWORD=dbc.local.123

# ===========================================
# 数据库 Migration 配置
# ===========================================

MIGRATION_DB_HOST=localhost
MIGRATION_DB_PORT=5433
MIGRATION_DB_NAME=dbc_local
MIGRATION_DB_SCHEMA=dbc
MIGRATION_USER=dbc_migration_writer
MIGRATION_PASSWORD=dbc.local.123

# ===========================================
# 腾讯云部署配置
# ===========================================

# 腾讯云凭证 (生产环境使用)
TENCENT_SECRET_ID=your-secret-id
TENCENT_SECRET_KEY=your-secret-key

# 部署区域
REGION=ap-chengdu

# Serverless 平台配置
SERVERLESS_PLATFORM_VENDOR=tencent

# ===========================================
# 环境标识
# ===========================================

# 当前环境 (development/production/test)
# Node.js 版本 (必须与腾讯云 Serverless 环境一致)
NODE_VERSION=20.19.5
NODE_ENV=development

# 部署阶段 (dev/prod)
STAGE=dev

# ===========================================
# 其他配置
# ===========================================

# 禁用 husky (CI 环境)
HUSKY=0

# 强制构建 (跳过变更检测)
FORCE_BUILD=false

# ===========================================
# 配置说明
# ===========================================
#
# 1. 非敏感配置：
#    - 直接修改 config/development.yaml 或 config/production.yaml
#    - 方便为不同环境配置不同的默认值
#
# 2. 敏感配置 (Secret)：
#    - 通过环境变量设置
#    - 在 GitHub Actions 中配置为 Secrets
#    - 在本地通过 .env 文件设置
#
# 3. 环境变量映射：
#    - 参考 config/custom-environment-variables.yaml
#    - 定义了环境变量到配置的映射关系
#
# 4. 使用方式：
#    - 本地开发: source .env
#    - CI/CD: 通过 GitHub Secrets 设置
#    - 生产环境: 通过云平台环境变量设置
#
# ===========================================
