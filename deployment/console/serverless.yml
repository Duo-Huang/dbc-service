# Console 服务配置
# 使用 SCF CLI 部署: scf deploy

component: scf
name: dbc-console
org: qypeak
app: dbc
stage: dev

inputs:
    name: dbc-console
    type: web
    src:
        src: ../../dist/apps/console
        exclude:
            - .env
    runtime: Nodejs20.19
    region: ${{ env:REGION }}
    memorySize: 512
    timeout: 30
    environment:
        variables:
            NODE_ENV: production
            CONSOLE_SERVER_PORT: 9000
            CONSOLE_DB_HOST: ${{ env:CONSOLE_DB_HOST }}
            CONSOLE_DB_PORT: ${{ env:CONSOLE_DB_PORT }}
            CONSOLE_DB_NAME: ${{ env:CONSOLE_DB_NAME }}
            CONSOLE_DB_SCHEMA: ${{ env:CONSOLE_DB_SCHEMA }}
            CONSOLE_DB_USER: ${{ env:CONSOLE_DB_USER }}
            CONSOLE_DB_PASSWORD: ${{ env:CONSOLE_DB_PASSWORD }}
    layers:
        - name: ${output:${stage}:${app}:dbc-deps-layer.name}
          version: ${output:${stage}:${app}:dbc-deps-layer.version}
    events:
        - http:
              parameters:
                  netConfig:
                      enableIntranet: true
                      enableExtranet: true
                  qualifier: $DEFAULT
                  authType: NONE
