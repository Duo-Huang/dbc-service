# Layer 配置 - 生产依赖管理
# 使用 SCF CLI 部署: scf deploy

component: layer
name: dbc-deps-layer
org: qypeak
app: dbc
stage: dev

inputs:
    name: dbc-deps-layer
    region: ap-chengdu
    src: ./node_modules
    targetDir: /node_modules
    runtimes:
        - Nodejs22.20.0
    description: DBC 项目生产依赖 Layer
    version: 1
