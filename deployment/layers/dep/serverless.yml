# Layer 配置 - 生产依赖管理
# 使用 SCF CLI 部署: scf deploy

component: layer
name: dbc-deps-layer
org: qypeak
app: dbc
stage: dev

inputs:
    name: dbc-deps-layer
    region: ${{ env:REGION }}
    src: ./node_modules
    targetDir: /node_modules
    runtimes:
        - Nodejs20.19
    description: DBC 项目生产依赖 Layer
